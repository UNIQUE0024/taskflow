# TaskFlow

A small task manager with a per-task cost estimate.

## Run it
Open `index.html` in your browser. No build step.

## Files
- `script.js` — task list logic
- `src/auth.js` — login form validation
- `src/budget.js` — cost/budget calculations
